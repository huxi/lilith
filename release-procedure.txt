Start gpg-agent:
gpg-agent --daemon --write-env-file "${HOME}/.gpg-agent-info"
. ~/.profile

Check that gpg-agent is running by calling gpg-agent without arguments.
#####

Validate correctness of trunk/lilith/lilith/src/main/resources/detailsView/history
Check that lilith logback.xml does not include debug="true"

#####

Search pom.xml for
<!-- SV -->
and replace with release version.
Don't forget <sulkyVersion>!

#####

Search pom.xml for
<!-- LV -->
and replace with release version.

#####

Remember to update changes.xml.

#####

Change into sulky root.
mvn3 clean install

#####

Change into lilith root.
mvn3 clean install

#####

Commit sulky with comment "x.x.x release"
Tag sulky with comment "x.x.x release".
git tag -s vx.x.x -m "x.x.x release"

#####

Commit lilith with comment "x.x.x release"
Tag lilith with comment "x.x.x release".
git tag -s vx.x.x -m "x.x.x release"

#####

rm -rf ~/local-gradle-repository/release
mkdir ~/local-gradle-repository/release

#####

Change into sulky root.
./gradlew -Drelease=true -C rebuild
./gradlew -Drelease=true -C rebuild javadocZip sourceZip

#####

Change into lilith root.
./gradlew -Drelease=true -C rebuild
./gradlew -Drelease=true -C rebuild javadocZip sourceZip
./gradlew syncStaging

#####

Validate staged files and release them:
https://oss.sonatype.org

#####

Upload javadocs

#####

Write release notes and changes (README.txt) for sf.net upload.

#####

Sign dists
gpg --local-user huxhorn@users.sf.net --armor --detach-sign <file>

#####

Upload lilith-files to sf.net
Use SFTP, Luke. m(

#####

Upload /releases/x.x.x.xhtml
Update current-version.txt

#####

Search pom.xml for
<!-- SV -->
and replace with next snapshot version.
Don't forget <sulkyVersion>!

#####

Search pom.xml for
<!-- LV -->
and replace with next snapshot version.

#####

Commit sulky root with comment "Starting work on x.x.x+1"
Commit lilith root with comment "Starting work on x.x.x+1"

#####

Push everything to the remote git repositories
git push origin
git push github
git push gitorious
git push --tags origin
git push --tags github
git push --tags gitorious

#####

Submit info to Freshmeat
http://freshmeat.net/projects/lilith-viewer/releases
Username: huxhorn

#####

Create new version (and default) and milestone in Trac.

#####

Send mail to lilith-announce@lists.sourceforge.net with CC to lilith-user@lists.sourceforge.net (using joern@lilith.huxhorn.de as sender)
Send mail to logback-user@qos.ch and slf4j-user@qos.ch (using jhuxhorn@googlemail.com as sender)
Twitter about the release @Lilith

